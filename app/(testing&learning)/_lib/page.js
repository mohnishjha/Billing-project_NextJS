import React from 'react'

const page = () => {
  return (
    <div>This is a private folder. You cannot see it's contents.

        But if you want to add an underscore in the url, we can use %5F which is url encoded form of an underscore
    </div>
  )
}

export default page